// app/help.tsx
import React from 'react';
import { View, Text, StyleSheet, ScrollView } from 'react-native';
import Footer from '@/components/Footer';

export default function HelpScreen() {
  return (
    <View style={styles.screen}>
      <ScrollView contentContainerStyle={styles.scroll}>
        <View style={styles.inner}>
          <Text style={styles.title}>Help &amp; Tips</Text>
          <Text style={styles.body}>
            • Use &quot;Create Schedule&quot; on the Home screen to build today&apos;s plan.{'\n'}
            • Once you press Finish, use the Edit Hub to refine each category.{'\n'}
            • Dream Team = staff working at B2 today.{'\n'}
            • Attending Participants = who is at Day Program today.{'\n'}
            • End of Shift Checklist = who is last to leave and responsible for closing tasks.
          </Text>
        </View>
      </ScrollView>
      <Footer />
    </View>
  );
}

const MAX_WIDTH = 880;

const styles = StyleSheet.create({
  screen: {
    flex: 1,
    backgroundColor: '#faf7fb',
  },
  scroll: {
    paddingVertical: 24,
    alignItems: 'center',
  },
  inner: {
    width: '100%',
    maxWidth: MAX_WIDTH,
    paddingHorizontal: 24,
  },
  title: {
    fontSize: 22,
    fontWeight: '700',
    marginBottom: 12,
    color: '#332244',
  },
  body: {
    fontSize: 14,
    lineHeight: 20,
    color: '#4c3b5c',
  },
});
